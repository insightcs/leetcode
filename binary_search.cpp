#include <iostream>
#include <vector>
using namespace std;


bool binary_search(vector<int>& nums, int start, int end)
{
    int mid = (start + end) / 2;
    if(nums[mid]==)
    {
        return true;
    }
    else if(nums[mid]>val)
    {
        end--;
    }
}
